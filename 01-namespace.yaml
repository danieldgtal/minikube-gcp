# This file defines a Kubernetes Namespace resource.
# Namespace configuration for web application demo

apiVersion: v1
kind: Namespace
metadata:
  name: webapp-demo
  labels:
    purpose: demo
